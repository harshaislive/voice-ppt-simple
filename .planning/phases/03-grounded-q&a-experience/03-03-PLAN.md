---
phase: 03-grounded-q&a-experience
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - public/app.js
  - public/services/socket.js
  - server/routes/autoplex.js
  - server/routes/questions.js
  - tests/runtime/qa-isolation.test.js
autonomous: true
requirements:
  - QA-03
must_haves:
  truths:
    - "Q&A mode owns transcript, status, and answer-audio state distinctly from narration mode."
    - "Q&A entry and exit do not leave stale narration transcript or overlapping runtime state behind."
    - "The presentation returns cleanly from Q&A to narration within the same UI lane."
  artifacts:
    - path: "public/app.js"
      provides: "Explicit Q&A mode ownership and cleanup in the browser orchestrator."
    - path: "public/services/socket.js"
      provides: "Stable Q&A lifecycle handling at the client runtime boundary."
    - path: "server/routes/autoplex.js"
      provides: "Q&A lifecycle events that remain compatible with Phase 2 playback rules."
    - path: "tests/runtime/qa-isolation.test.js"
      provides: "Automated verification for non-overlapping Q&A/narration state transitions."
  key_links:
    - from: "server/routes/autoplex.js"
      to: "public/services/socket.js"
      via: "qa-start / qa-end lifecycle controls client mode boundaries"
      pattern: "qa-start|qa-end|answer-delta|answer-text"
    - from: "public/services/socket.js"
      to: "public/app.js"
      via: "Q&A events drive transcript/status/audio cleanup and restoration"
      pattern: "isQAPhase|restorePresentationStatus|handleNarrationDelta"
---

<objective>
Finish Phase 3 by making Q&A a distinct runtime mode instead of a blurry extension of narration.

Purpose: Implement D-11 through D-13 so Q&A owns its transcript/audio/status lifecycle, then returns cleanly to narration without overlap or stale state.
Output: Explicit Q&A mode boundaries and focused regression coverage for clean enter/exit behavior.
</objective>

<execution_context>
@/home/harsha-mudumba/.codex/get-shit-done/workflows/execute-plan.md
@/home/harsha-mudumba/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-grounded-q&a-experience/03-CONTEXT.md
@.planning/phases/03-grounded-q&a-experience/03-RESEARCH.md
@.planning/phases/03-grounded-q&a-experience/03-PATTERNS.md
@.planning/phases/02-playback-state-reliability/02-CONTEXT.md
@public/app.js
@public/services/socket.js
@server/routes/autoplex.js
@server/routes/questions.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Q&A isolation regression coverage</name>
  <read_first>
    - public/app.js
    - public/services/socket.js
    - server/routes/autoplex.js
    - .planning/phases/03-grounded-q&a-experience/03-CONTEXT.md
    - .planning/phases/02-playback-state-reliability/02-CONTEXT.md
  </read_first>
  <files>tests/runtime/qa-isolation.test.js</files>
  <behavior>
    - Test 1: `qa-start` moves the client into an explicit Q&A state with distinct status/transcript ownership.
    - Test 2: answer streaming and answer completion do not leave stale narration transcript or overlap state behind.
    - Test 3: `qa-end` restores presentation status cleanly without replaying or corrupting narration state.
  </behavior>
  <action>Create focused runtime coverage for Q&A state isolation using the existing client/socket orchestration paths. Keep the tests anchored to the Phase 2 playback contract by checking mode boundaries and cleanup rather than duplicating lower-level playback tests.</action>
  <acceptance_criteria>
    - `test -f tests/runtime/qa-isolation.test.js` succeeds.
    - `rg -n "qa-start|qa-end|isQAPhase|restorePresentationStatus|transcript" tests/runtime/qa-isolation.test.js public/services/socket.js public/app.js` returns isolation assertions.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand tests/runtime/qa-isolation.test.js</automated>
  </verify>
  <done>The repo has focused coverage that fails when Q&A and narration state boundaries blur again.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Isolate Q&A transcript, status, and answer-mode lifecycle</name>
  <read_first>
    - public/app.js
    - public/services/socket.js
    - server/routes/autoplex.js
    - server/routes/questions.js
    - tests/runtime/qa-isolation.test.js
    - .planning/phases/03-grounded-q&a-experience/03-RESEARCH.md
  </read_first>
  <files>public/app.js, public/services/socket.js, server/routes/autoplex.js, server/routes/questions.js</files>
  <action>Refactor Q&A mode so it owns its own transcript/status/audio lifecycle inside the existing presentation lane, then returns cleanly to narration without stale text, overlapping state, or accidental replay behavior. Preserve the current Socket.IO Q&A event flow and the Phase 2 playback ownership rules; do not invent a second client runtime path for answers.</action>
  <acceptance_criteria>
    - `rg -n "qa-start|qa-end|answer-delta|answer-text|isQAPhase|restorePresentationStatus" public/app.js public/services/socket.js server/routes/autoplex.js server/routes/questions.js` shows explicit Q&A mode handling.
    - `npm test -- --runInBand tests/runtime/qa-isolation.test.js` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand tests/runtime/qa-isolation.test.js</automated>
  </verify>
  <done>Q&A has explicit runtime ownership and no longer leaves the presentation in a blurred or inconsistent state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| narration mode -> Q&A mode | Mode transition must preserve Phase 2 playback guarantees and avoid stale carry-over |
| Q&A mode -> narration mode | Answer lifecycle must end cleanly before presentation status/transcript ownership returns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | T | Q&A runtime state | mitigate | Give Q&A explicit mode ownership with regression-tested cleanup and restoration |
| T-03-06 | D | shared transcript/audio surfaces | mitigate | Prevent overlap and stale state by making enter/exit transitions explicit |
</threat_model>

<verification>
Run the Q&A isolation suite and confirm Q&A owns its runtime state distinctly, then restores narration mode cleanly in the same UI lane.
</verification>

<success_criteria>
Q&A and narration are behaviorally distinct without a new surface:
- Q&A owns status/transcript/audio during answer mode
- answer mode exits cleanly
- narration state remains stable after Q&A completes
</success_criteria>

<output>
After completion, create `.planning/phases/03-grounded-q&a-experience/03-grounded-q&a-experience-03-SUMMARY.md`
</output>
