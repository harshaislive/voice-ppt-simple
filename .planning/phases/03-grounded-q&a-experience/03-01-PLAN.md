---
phase: 03-grounded-q&a-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/routes/questions.js
  - server/prompts/narrationPrompt.js
  - server/services/questionClassifier.js
  - tests/helpers/createQuestionFlowHarness.js
  - tests/runtime/qa-grounding.test.js
autonomous: true
requirements:
  - QA-01
must_haves:
  truths:
    - "Q&A answers are grounded slide-first instead of full-deck-first."
    - "Project knowledge documents support answers, but do not silently expand the answer boundary."
    - "Auto-interrupt is reserved for explicit urgent questions, not generic slide relevance."
  artifacts:
    - path: "server/routes/questions.js"
      provides: "Slide-first answer context assembly and grounded fallback handling."
    - path: "server/prompts/narrationPrompt.js"
      provides: "Prompt-level grounding contract for honest, bounded answers."
    - path: "server/services/questionClassifier.js"
      provides: "Interrupt policy aligned to urgent-only auto-answering."
    - path: "tests/runtime/qa-grounding.test.js"
      provides: "Automated verification for grounding boundaries and interrupt criteria."
  key_links:
    - from: "server/routes/questions.js"
      to: "server/prompts/narrationPrompt.js"
      via: "answer context delivered to the Q&A prompt"
      pattern: "knowledgeContext|QA_SYSTEM_PROMPT"
    - from: "server/services/questionClassifier.js"
      to: "server/routes/autoplex.js"
      via: "priority and shouldAnswerNow shape inline interruption"
      pattern: "shouldAnswerNow|interrupt"
---

<objective>
Tighten the Q&A backend contract before touching UI polish or playback isolation.

Purpose: Implement D-01 through D-04 and D-08 through D-10 first so answers stay grounded and interruptions reflect urgency instead of generic relevance.
Output: Slide-first answer context, honest unsupported-answer behavior, and a narrower interrupt policy backed by focused tests.
</objective>

<execution_context>
@/home/harsha-mudumba/.codex/get-shit-done/workflows/execute-plan.md
@/home/harsha-mudumba/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-grounded-q&a-experience/03-CONTEXT.md
@.planning/phases/03-grounded-q&a-experience/03-RESEARCH.md
@.planning/phases/03-grounded-q&a-experience/03-PATTERNS.md
@server/routes/questions.js
@server/prompts/narrationPrompt.js
@server/services/questionClassifier.js
@server/routes/autoplex.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add grounding and interrupt regression coverage</name>
  <read_first>
    - server/routes/questions.js
    - server/prompts/narrationPrompt.js
    - server/services/questionClassifier.js
    - .planning/phases/03-grounded-q&a-experience/03-CONTEXT.md
    - .planning/phases/03-grounded-q&a-experience/03-PATTERNS.md
  </read_first>
  <files>tests/helpers/createQuestionFlowHarness.js, tests/runtime/qa-grounding.test.js</files>
  <behavior>
    - Test 1: answer context keeps current slide content primary and does not default to full-deck reasoning.
    - Test 2: unsupported questions produce an honest bounded-answer path rather than broad inferred answers.
    - Test 3: relevant-but-non-urgent questions can stay queued while objections, contradictions, pricing, and next-step asks remain interrupt-worthy.
  </behavior>
  <action>Create a minimal Q&A harness that exercises question context assembly and classifier decisions without booting the full browser runtime. Follow the exact route/service boundaries mapped in `03-PATTERNS.md`: keep `server/routes/questions.js` as the answer-context edge, keep `server/services/questionClassifier.js` as the urgency gate, and keep the tests focused on grounding and interrupt rules rather than full UI behavior.</action>
  <acceptance_criteria>
    - `test -f tests/helpers/createQuestionFlowHarness.js` succeeds.
    - `test -f tests/runtime/qa-grounding.test.js` succeeds.
    - `rg -n "slide-first|unsupported|pricing|next step|shouldAnswerNow" tests/runtime/qa-grounding.test.js` returns grounding and interrupt assertions.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand tests/runtime/qa-grounding.test.js</automated>
  </verify>
  <done>The repo has focused Q&A grounding coverage that fails when context boundaries or interrupt policy drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tighten slide-first answer context and urgent-only interrupt policy</name>
  <read_first>
    - server/routes/questions.js
    - server/prompts/narrationPrompt.js
    - server/services/questionClassifier.js
    - tests/runtime/qa-grounding.test.js
    - .planning/phases/03-grounded-q&a-experience/03-RESEARCH.md
  </read_first>
  <files>server/routes/questions.js, server/prompts/narrationPrompt.js, server/services/questionClassifier.js</files>
  <action>Refactor Q&A generation so answers are grounded slide-first, use project docs only as explicit support, and fail honestly when support is missing. At the same time, decouple generic slide relevance from automatic interruption so only explicit urgent question types interrupt by default. Preserve the current brownfield route and prompt boundaries; do not introduce a second answer-generation pipeline.</action>
  <acceptance_criteria>
    - `rg -n "CURRENT SLIDE|PROJECT|presentation content|unsupported|shouldAnswerNow|priority" server/routes/questions.js server/prompts/narrationPrompt.js server/services/questionClassifier.js` shows explicit grounding and urgency logic.
    - `npm test -- --runInBand tests/runtime/qa-grounding.test.js` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand tests/runtime/qa-grounding.test.js</automated>
  </verify>
  <done>Q&A answers are grounded to the right scope, and the presentation only auto-interrupts for genuinely urgent questions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| slide context -> model answer | The model must not treat broad deck/project context as equal evidence for every answer |
| question priority -> live interruption | Important questions must not automatically become inline interrupts unless they match the urgent policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I | Q&A grounding | mitigate | Enforce slide-first context and honest unsupported-answer behavior |
| T-03-02 | D | interrupt policy | mitigate | Split urgency from generic relevance and regression-test urgent-only interruption |
</threat_model>

<verification>
Run the grounding suite and confirm answers stay slide-first, unsupported asks fail honestly, and only explicit urgent questions trigger inline interruption.
</verification>

<success_criteria>
Phase 3 grounding foundation is in place:
- current slide remains the primary answer boundary
- project docs act as support, not silent expansion
- interruption policy is urgent-only by default
</success_criteria>

<output>
After completion, create `.planning/phases/03-grounded-q&a-experience/03-grounded-q&a-experience-01-SUMMARY.md`
</output>
