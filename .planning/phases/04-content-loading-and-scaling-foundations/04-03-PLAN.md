---
phase: 04-content-loading-and-scaling-foundations
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - server/services/cms.js
  - server/routes/cms.js
  - content/projects
  - tests/runtime/content-project-package.test.js
autonomous: true
requirements:
  - CMS-02
must_haves:
  truths:
    - "New presentations follow a project-first content package pattern."
    - "Catalog and loader behavior derive from project package metadata instead of hardcoded presentation assumptions."
    - "Adding a new project package does not require edits to core loading logic."
  artifacts:
    - path: "server/services/cms.js"
      provides: "Project-package-aware listing and load behavior."
    - path: "server/routes/cms.js"
      provides: "Read surfaces that expose package-derived presentation metadata."
    - path: "tests/runtime/content-project-package.test.js"
      provides: "Automated verification for project-package onboarding behavior."
  key_links:
    - from: "content/projects/*"
      to: "server/services/cms.js"
      via: "package metadata drives catalog and load behavior"
      pattern: "project.json|presentations"
    - from: "server/services/cms.js"
      to: "server/routes/cms.js"
      via: "package-derived metadata is surfaced through CMS read routes"
      pattern: "listProjectPresentations|loadProject"
---

<objective>
Standardize the scaling path for new client-facing presentations.

Purpose: Implement D-07 through D-09 so new content can be added by package structure and metadata rather than code surgery.
Output: A stable project-package loading path, example expectations, and focused onboarding tests.
</objective>

<execution_context>
@/home/harsha-mudumba/.codex/get-shit-done/workflows/execute-plan.md
@/home/harsha-mudumba/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-content-loading-and-scaling-foundations/04-CONTEXT.md
@.planning/phases/04-content-loading-and-scaling-foundations/04-RESEARCH.md
@.planning/phases/04-content-loading-and-scaling-foundations/04-PATTERNS.md
@server/services/cms.js
@server/routes/cms.js
@content/projects/beforest/project.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add project-package onboarding regression coverage</name>
  <read_first>
    - server/services/cms.js
    - server/routes/cms.js
    - content/projects/beforest/project.json
    - .planning/phases/04-content-loading-and-scaling-foundations/04-RESEARCH.md
  </read_first>
  <files>tests/runtime/content-project-package.test.js</files>
  <behavior>
    - Test 1: a well-formed project package appears in the catalog with the right project and presentation metadata.
    - Test 2: package-driven presentation loading pulls project docs and presentation content without special-case code.
    - Test 3: malformed or incomplete packages fail in a diagnosable way instead of half-loading silently.
  </behavior>
  <action>Create focused tests around the existing `content/projects` package model so the scaling path is protected by automated coverage. Keep the test scope on listing and loading behavior rather than UI rendering.</action>
  <acceptance_criteria>
    - `test -f tests/runtime/content-project-package.test.js` succeeds.
    - `rg -n "project.json|presentations|projectSlug|presentationSlug" tests/runtime/content-project-package.test.js` returns package-shape assertions.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand tests/runtime/content-project-package.test.js</automated>
  </verify>
  <done>The repo has tests that protect the project-package onboarding model from regressing into code-coupled behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make project packages the default scaling path for local content</name>
  <read_first>
    - server/services/cms.js
    - server/routes/cms.js
    - content/projects/beforest/project.json
    - tests/runtime/content-project-package.test.js
  </read_first>
  <files>server/services/cms.js, server/routes/cms.js</files>
  <action>Refactor project listing and load behavior so `content/projects/<project>` acts as the first-class local package model for new presentations. Preserve legacy local deck support where necessary, but make package-derived metadata the default path for catalog and content loading.</action>
  <acceptance_criteria>
    - `rg -n "project.json|presentations|projectSlug|defaultPresentation|source" server/services/cms.js server/routes/cms.js` shows package-aware loading and metadata surfacing.
    - `npm test -- --runInBand tests/runtime/content-project-package.test.js` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand tests/runtime/content-project-package.test.js</automated>
  </verify>
  <done>New local presentations can be added through a repeatable project package pattern instead of core logic edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project package metadata -> runtime catalog | Package metadata must be complete enough for loader and catalog decisions |
| onboarding convention -> loader behavior | New content should succeed through metadata, not through code-path accidents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | I | onboarding model | mitigate | Standardize package-driven listing/loading and regression-test malformed package cases |
| T-04-06 | D | local content scaling | mitigate | Make project packages the default local scaling path instead of brittle special cases |
</threat_model>

<verification>
Run the content-project-package suite and confirm new project packages list and load correctly through metadata-driven behavior.
</verification>

<success_criteria>
The scaling path is clearer:
- new presentations use a project-first package model
- catalog and load behavior derive from package metadata
- onboarding avoids core-code surgery
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-loading-and-scaling-foundations/04-content-loading-and-scaling-foundations-03-SUMMARY.md`
</output>
