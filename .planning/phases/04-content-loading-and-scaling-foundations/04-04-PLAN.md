---
phase: 04-content-loading-and-scaling-foundations
plan: 04
type: execute
wave: 4
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified:
  - server/services/cms.js
  - server/routes/session.js
  - public/app.js
  - server/middleware/logger.js
  - tests/runtime/content-fail-closed.test.js
autonomous: true
requirements:
  - CMS-03
must_haves:
  truths:
    - "Wrong-source or missing-content failures are explicit to operators."
    - "Client bootstrap no longer silently chooses an arbitrary presentation when selection is unresolved."
    - "Structured diagnostics make content-loading failures explainable in production."
  artifacts:
    - path: "server/services/cms.js"
      provides: "Structured mismatch and fail-closed diagnostics."
    - path: "server/routes/session.js"
      provides: "Operator-visible startup error contract."
    - path: "public/app.js"
      provides: "Client bootstrap behavior that respects explicit presentation choice."
    - path: "tests/runtime/content-fail-closed.test.js"
      provides: "Automated verification for fail-closed behavior and diagnostics."
  key_links:
    - from: "public/app.js"
      to: "server/routes/session.js"
      via: "client selection and startup error handling must align on explicit slug resolution"
      pattern: "presentationCatalog|startSession|error"
    - from: "server/services/cms.js"
      to: "server/middleware/logger.js"
      via: "structured logs explain mismatch and fail-closed outcomes"
      pattern: "event|source|presentationSlug|reason"
---

<objective>
Finish Phase 4 by making wrong-source behavior loudly diagnosable instead of quietly survivable.

Purpose: Reinforce D-10 through D-12 with explicit operator diagnostics and client bootstrap behavior that respects resolved selection instead of arbitrary fallback.
Output: Clear fail-closed errors, aligned client/server messaging, and focused diagnostics tests.
</objective>

<execution_context>
@/home/harsha-mudumba/.codex/get-shit-done/workflows/execute-plan.md
@/home/harsha-mudumba/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-content-loading-and-scaling-foundations/04-CONTEXT.md
@.planning/phases/04-content-loading-and-scaling-foundations/04-RESEARCH.md
@.planning/phases/04-content-loading-and-scaling-foundations/04-PATTERNS.md
@server/services/cms.js
@server/routes/session.js
@public/app.js
@server/middleware/logger.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fail-closed diagnostics regression coverage</name>
  <read_first>
    - server/services/cms.js
    - server/routes/session.js
    - public/app.js
    - server/middleware/logger.js
    - .planning/phases/04-content-loading-and-scaling-foundations/04-RESEARCH.md
  </read_first>
  <files>tests/runtime/content-fail-closed.test.js</files>
  <behavior>
    - Test 1: client bootstrap does not auto-select a different presentation when the requested one is missing or mismatched.
    - Test 2: session-start failures expose clear operator-facing reason fields for wrong-source and missing-content cases.
    - Test 3: structured logs or logger payloads include requested slug, expected source, and failure reason.
  </behavior>
  <action>Create focused runtime tests for diagnostics and fail-closed behavior across the client bootstrap and session-start boundary. Keep the tests narrow to selection and error/reporting behavior rather than replaying all earlier content-resolution logic.</action>
  <acceptance_criteria>
    - `test -f tests/runtime/content-fail-closed.test.js` succeeds.
    - `rg -n "missing|mismatch|requested|expected|reason" tests/runtime/content-fail-closed.test.js` returns explicit diagnostics assertions.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand tests/runtime/content-fail-closed.test.js</automated>
  </verify>
  <done>The repo has tests that fail when content loading becomes silently permissive again.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align client bootstrap and operator diagnostics with fail-closed content loading</name>
  <read_first>
    - server/services/cms.js
    - server/routes/session.js
    - public/app.js
    - server/middleware/logger.js
    - tests/runtime/content-fail-closed.test.js
  </read_first>
  <files>server/services/cms.js, server/routes/session.js, public/app.js, server/middleware/logger.js</files>
  <action>Refine client and server behavior so explicit presentation selection is preserved, arbitrary catalog fallback is removed from critical paths, and wrong-source startup returns structured, operator-readable diagnostics. Preserve brownfield usability by keeping non-critical browsing/catalog flows functional, but stop masking startup mistakes.</action>
  <acceptance_criteria>
    - `rg -n "error|mismatch|requested|expected|reason|presentationCatalog" server/services/cms.js server/routes/session.js public/app.js server/middleware/logger.js` shows aligned diagnostics and reduced arbitrary fallback.
    - `npm test -- --runInBand tests/runtime/content-fail-closed.test.js` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand tests/runtime/content-fail-closed.test.js</automated>
  </verify>
  <done>Operators and client bootstrap now see explicit content-loading failures instead of silent substitution behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unresolved client selection -> session-start request | Client should not invent a different target presentation silently |
| fail-closed server decision -> operator response | Server diagnostics must be clear enough to act on in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | I | operator diagnostics | mitigate | Surface requested slug, expected source, and mismatch reason consistently |
| T-04-08 | D | client bootstrap | mitigate | Remove arbitrary startup fallback and regression-test explicit selection behavior |
</threat_model>

<verification>
Run the content-fail-closed suite and confirm client bootstrap, server startup, and diagnostics all behave explicitly when presentation content cannot be trusted.
</verification>

<success_criteria>
Content-loading failures are now operationally useful:
- no arbitrary startup substitution
- startup failures explain what went wrong
- structured diagnostics point to the source mismatch clearly
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-loading-and-scaling-foundations/04-content-loading-and-scaling-foundations-04-SUMMARY.md`
</output>
