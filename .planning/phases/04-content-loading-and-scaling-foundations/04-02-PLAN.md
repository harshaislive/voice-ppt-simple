---
phase: 04-content-loading-and-scaling-foundations
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - server/routes/session.js
  - public/app.js
  - server/services/masterSession.js
  - server/services/supabaseSession.js
  - server/routes/questions.js
  - server/routes/autoplex.js
  - tests/runtime/content-identity.test.js
autonomous: true
requirements:
  - CMS-01
must_haves:
  truths:
    - "Presentation slug becomes the canonical runtime identity."
    - "Legacy `deckId` and `deck_id` fields remain compatible while readers move to canonical metadata."
    - "Downstream reload and cache paths stop using `projectSlug` as an implicit presentation identifier."
  artifacts:
    - path: "server/routes/session.js"
      provides: "Canonical presentation identity at session start."
    - path: "public/app.js"
      provides: "Client selection and persistence aligned to presentation slug."
    - path: "server/services/masterSession.js"
      provides: "Master-session lookup aligned to canonical slug."
    - path: "tests/runtime/content-identity.test.js"
      provides: "Automated verification for canonical identity and compatibility fields."
  key_links:
    - from: "public/app.js"
      to: "server/routes/session.js"
      via: "selected presentation slug becomes the startup identifier"
      pattern: "presentationSlug|deckId|persistSession"
    - from: "server/routes/session.js"
      to: "server/routes/questions.js"
      via: "session metadata drives downstream presentation reloads"
      pattern: "presentationSlug|projectSlug|deck_id"
---

<objective>
Normalize runtime identity after source selection is explicit.

Purpose: Implement D-04 through D-06 so presentation slug becomes the stable runtime contract without breaking brownfield readers in one step.
Output: Canonical identity mapping across client, session metadata, and downstream services backed by focused tests.
</objective>

<execution_context>
@/home/harsha-mudumba/.codex/get-shit-done/workflows/execute-plan.md
@/home/harsha-mudumba/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-content-loading-and-scaling-foundations/04-CONTEXT.md
@.planning/phases/04-content-loading-and-scaling-foundations/04-RESEARCH.md
@.planning/phases/04-content-loading-and-scaling-foundations/04-PATTERNS.md
@server/routes/session.js
@public/app.js
@server/services/masterSession.js
@server/services/supabaseSession.js
@server/routes/questions.js
@server/routes/autoplex.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical identity regression coverage</name>
  <read_first>
    - server/routes/session.js
    - public/app.js
    - server/services/masterSession.js
    - server/routes/questions.js
    - server/routes/autoplex.js
  </read_first>
  <files>tests/runtime/content-identity.test.js</files>
  <behavior>
    - Test 1: session startup persists canonical `presentationSlug` and `projectSlug` metadata while keeping compatibility `deckId` fields populated.
    - Test 2: downstream reload paths prefer `presentationSlug` before legacy aliases.
    - Test 3: `projectSlug` is not used as a fallback presentation identifier in canonical paths.
  </behavior>
  <action>Create focused runtime tests around identity propagation and compatibility fields. Keep the tests anchored to the existing session metadata and loader behavior rather than adding a new global ID abstraction.</action>
  <acceptance_criteria>
    - `test -f tests/runtime/content-identity.test.js` succeeds.
    - `rg -n "presentationSlug|deckId|projectSlug|deck_id" tests/runtime/content-identity.test.js` returns canonical identity assertions.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand tests/runtime/content-identity.test.js</automated>
  </verify>
  <done>The repo has focused tests that fail when runtime identity drifts back to mixed implicit fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagate canonical presentation identity across client and server</name>
  <read_first>
    - server/routes/session.js
    - public/app.js
    - server/services/masterSession.js
    - server/services/supabaseSession.js
    - server/routes/questions.js
    - server/routes/autoplex.js
    - tests/runtime/content-identity.test.js
  </read_first>
  <files>server/routes/session.js, public/app.js, server/services/masterSession.js, server/services/supabaseSession.js, server/routes/questions.js, server/routes/autoplex.js</files>
  <action>Refactor the runtime to prefer canonical `presentationSlug` end to end while maintaining temporary compatibility with `deckId` and persisted `deck_id`. Remove `projectSlug` as an implicit presentation fallback in reload paths, and keep compatibility behavior explicit rather than accidental.</action>
  <acceptance_criteria>
    - `rg -n "presentationSlug|deckId|projectSlug|deck_id" server/routes/session.js public/app.js server/services/masterSession.js server/services/supabaseSession.js server/routes/questions.js server/routes/autoplex.js` shows canonical-first identity handling.
    - `npm test -- --runInBand tests/runtime/content-identity.test.js` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand tests/runtime/content-identity.test.js</automated>
  </verify>
  <done>Presentation slug is now the canonical runtime identity, with legacy aliases preserved intentionally rather than implicitly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-selected presentation -> server session metadata | Selection must stay canonical across bootstrap and restore |
| session metadata -> downstream reloads | Replay/Q&A/master lookup paths must not reinterpret identity differently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | T | runtime identity | mitigate | Prefer `presentationSlug` end to end with compatibility tests for old aliases |
| T-04-04 | I | downstream reloads | mitigate | Remove `projectSlug` as a fallback presentation ID and regression-test reload behavior |
</threat_model>

<verification>
Run the content-identity suite and confirm canonical presentation slug flows through session startup, persistence, and downstream reloads without breaking legacy compatibility.
</verification>

<success_criteria>
Runtime identity becomes unambiguous:
- presentation slug is canonical
- compatibility fields remain intentional and bounded
- downstream loaders stop guessing from project slug
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-loading-and-scaling-foundations/04-content-loading-and-scaling-foundations-02-SUMMARY.md`
</output>
