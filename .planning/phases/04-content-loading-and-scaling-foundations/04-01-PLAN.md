---
phase: 04-content-loading-and-scaling-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/cms.js
  - server/routes/session.js
  - server/routes/cms.js
  - tests/helpers/createContentLoadingHarness.js
  - tests/runtime/content-source-resolution.test.js
autonomous: true
requirements:
  - CMS-01
  - CMS-03
must_haves:
  truths:
    - "Presentation load follows an explicit declared source instead of opportunistic hybrid fallback."
    - "Session start rejects wrong-source or unresolved presentation requests instead of starting with mismatched content."
    - "Loader and route surfaces expose enough metadata to diagnose source selection decisions."
  artifacts:
    - path: "server/services/cms.js"
      provides: "Explicit source-resolution contract and mismatch detection."
    - path: "server/routes/session.js"
      provides: "Fail-closed session-start behavior for presentation resolution."
    - path: "server/routes/cms.js"
      provides: "Consistent catalog/read metadata and load-error surfacing."
    - path: "tests/runtime/content-source-resolution.test.js"
      provides: "Automated verification for explicit source selection and wrong-source rejection."
  key_links:
    - from: "server/services/cms.js"
      to: "server/routes/session.js"
      via: "resolved presentation source and mismatch errors drive startup behavior"
      pattern: "loadPresentation|source|presentationSlug"
    - from: "server/services/cms.js"
      to: "server/routes/cms.js"
      via: "catalog and read routes surface declared and resolved source metadata"
      pattern: "listPresentations|loadPresentation"
---

<objective>
Turn the content loader into an explicit source contract before normalizing identity elsewhere.

Purpose: Implement D-01 through D-03 and D-10 through D-12 first so the system stops “helpfully” loading the wrong content.
Output: Explicit source selection, fail-closed session start, and focused regression coverage.
</objective>

<execution_context>
@/home/harsha-mudumba/.codex/get-shit-done/workflows/execute-plan.md
@/home/harsha-mudumba/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-content-loading-and-scaling-foundations/04-CONTEXT.md
@.planning/phases/04-content-loading-and-scaling-foundations/04-RESEARCH.md
@.planning/phases/04-content-loading-and-scaling-foundations/04-PATTERNS.md
@server/services/cms.js
@server/routes/session.js
@server/routes/cms.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add source-resolution regression coverage</name>
  <read_first>
    - server/services/cms.js
    - server/routes/session.js
    - server/routes/cms.js
    - .planning/phases/04-content-loading-and-scaling-foundations/04-CONTEXT.md
    - .planning/phases/04-content-loading-and-scaling-foundations/04-PATTERNS.md
  </read_first>
  <files>tests/helpers/createContentLoadingHarness.js, tests/runtime/content-source-resolution.test.js</files>
  <behavior>
    - Test 1: loader respects explicit source declaration and does not silently swap to another source.
    - Test 2: session start rejects presentation startup when declared source content cannot be resolved.
    - Test 3: catalog/read surfaces expose enough source metadata to explain what was selected.
  </behavior>
  <action>Create a minimal content-loading harness that exercises CMS loader and session-start behavior without booting the full browser runtime. Keep the harness centered on `server/services/cms.js` and `server/routes/session.js` rather than inventing a second loading abstraction.</action>
  <acceptance_criteria>
    - `test -f tests/helpers/createContentLoadingHarness.js` succeeds.
    - `test -f tests/runtime/content-source-resolution.test.js` succeeds.
    - `rg -n "source|fail closed|presentationSlug|mismatch" tests/runtime/content-source-resolution.test.js` returns source-contract assertions.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand tests/runtime/content-source-resolution.test.js</automated>
  </verify>
  <done>The repo has focused tests that fail when explicit source rules drift back to silent fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce declared source rules at loader and session-start boundaries</name>
  <read_first>
    - server/services/cms.js
    - server/routes/session.js
    - server/routes/cms.js
    - tests/runtime/content-source-resolution.test.js
    - .planning/phases/04-content-loading-and-scaling-foundations/04-RESEARCH.md
  </read_first>
  <files>server/services/cms.js, server/routes/session.js, server/routes/cms.js</files>
  <action>Refactor content loading so the runtime resolves a presentation through an explicit source contract, surfaces mismatch reasons, and fails closed on session startup when the requested source cannot satisfy the request. Preserve the shared CMS service as the central seam; do not scatter source-selection logic into multiple routes.</action>
  <acceptance_criteria>
    - `rg -n "source|declared|resolved|mismatch|presentationSlug" server/services/cms.js server/routes/session.js server/routes/cms.js` shows explicit source enforcement and diagnostics.
    - `npm test -- --runInBand tests/runtime/content-source-resolution.test.js` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand tests/runtime/content-source-resolution.test.js</automated>
  </verify>
  <done>Loader and startup boundaries now honor declared source-of-truth rules instead of opportunistic fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| requested presentation -> resolved content source | Requested slug must not silently resolve to a different source than the declared contract |
| loader result -> session startup | Session creation must not proceed on missing or mismatched content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | source resolution | mitigate | Enforce explicit source declaration and regression-test wrong-source rejection |
| T-04-02 | I | session startup | mitigate | Fail closed when presentation content cannot be resolved from the declared source |
</threat_model>

<verification>
Run the source-resolution suite and confirm the runtime honors declared source, returns clear mismatch errors, and refuses to start a mismatched session.
</verification>

<success_criteria>
Phase 4 source foundation is in place:
- each presentation resolves through an explicit source contract
- wrong-source startup is blocked
- source decisions are visible in logs and responses
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-loading-and-scaling-foundations/04-content-loading-and-scaling-foundations-01-SUMMARY.md`
</output>
