---
phase: 02-playback-state-reliability
plan: 04
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - public/app.js
  - public/services/socket.js
  - server/routes/session.js
  - server/routes/autoplex.js
  - tests/runtime/playback-restore.test.js
autonomous: true
requirements:
  - PLAY-04
must_haves:
  truths:
    - "Reconnect restores state and control without auto-replaying narration."
    - "Reconnect does not auto-resume current-slide playback."
    - "Stale playback state is cleared before the controller explicitly resumes or replays."
  artifacts:
    - path: "public/app.js"
      provides: "Conservative restore behavior and stale-state cleanup."
    - path: "server/routes/session.js"
      provides: "Restore payloads and session state consumption aligned to conservative reconnect semantics."
    - path: "tests/runtime/playback-restore.test.js"
      provides: "Regression coverage for reconnect and restore behavior."
  key_links:
    - from: "public/app.js"
      to: "server/routes/session.js"
      via: "restore uses server state but avoids auto-replay/auto-resume"
      pattern: "restore|resume|replay"
    - from: "public/services/socket.js"
      to: "server/routes/autoplex.js"
      via: "reconnect restores control authority without hidden playback restart"
      pattern: "connect|session-joined"
---

<objective>
Finish Phase 2 by making reconnect and restore state-safe instead of aggressively automatic.

Purpose: Implement D-11 through D-13 so refreshes, reconnects, and session restores do not trigger stale or duplicate narration.
Output: Conservative restore semantics and focused regression coverage for reconnect behavior.
</objective>

<execution_context>
@/home/harsha-mudumba/.codex/get-shit-done/workflows/execute-plan.md
@/home/harsha-mudumba/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-playback-state-reliability/02-CONTEXT.md
@.planning/phases/02-playback-state-reliability/02-RESEARCH.md
@.planning/phases/02-playback-state-reliability/02-PATTERNS.md
@public/app.js
@public/services/socket.js
@server/routes/session.js
@server/routes/autoplex.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reconnect and restore regression coverage</name>
  <read_first>
    - public/app.js
    - public/services/socket.js
    - server/routes/session.js
    - server/routes/autoplex.js
    - .planning/phases/02-playback-state-reliability/02-PATTERNS.md
  </read_first>
  <files>tests/runtime/playback-restore.test.js</files>
  <behavior>
    - Test 1: restore reconnects the controller to the current session state without auto-replaying the current slide.
    - Test 2: restore does not auto-resume narration after reconnect.
    - Test 3: explicit replay or resume still works after restore and uses the correct current slide/control authority.
  </behavior>
  <action>Add a focused restore suite around the current `restoreSession()` path, socket reconnect readiness, and session route payloads. Follow `02-PATTERNS.md`: use `public/app.js` as the restore orchestrator, `public/services/socket.js` as the reconnect contract edge, and `server/routes/session.js` as the authoritative state source.</action>
  <acceptance_criteria>
    - `test -f tests/runtime/playback-restore.test.js` succeeds.
    - `rg -n "restore|auto-replay|auto-resume|explicit" tests/runtime/playback-restore.test.js` returns meaningful assertions.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand tests/runtime/playback-restore.test.js</automated>
  </verify>
  <done>The restore and reconnect rules are captured in focused regression tests before behavior changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make restore conservative and clear stale playback state</name>
  <read_first>
    - public/app.js
    - public/services/socket.js
    - server/routes/session.js
    - server/routes/autoplex.js
    - tests/runtime/playback-restore.test.js
  </read_first>
  <files>public/app.js, public/services/socket.js, server/routes/session.js, server/routes/autoplex.js</files>
  <action>Refactor restore/reconnect so it hydrates current session state and control authority without replaying or resuming narration automatically. Clear stale browser playback state before allowing explicit replay/resume. Preserve the current session route and socket join boundaries; do not invent a second restore flow.</action>
  <acceptance_criteria>
    - `rg -n "restoreSession|replaySlide|pauseAutoplex|session-joined" public/app.js public/services/socket.js server/routes/session.js server/routes/autoplex.js` reflects conservative restore handling.
    - `npm test -- --runInBand tests/runtime/playback-restore.test.js` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand tests/runtime/playback-restore.test.js</automated>
  </verify>
  <done>Reconnect and restore are safe, conservative, and no longer restart narration without explicit controller intent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| restored browser session -> active presentation state | Restore must recover authority without re-triggering stale playback |
| reconnect transport recovery -> explicit controller action | Reconnect must not be mistaken for replay or resume intent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | T | restore semantics | mitigate | Make restore state-only and clear stale playback before explicit next action |
| T-02-08 | D | reconnect behavior | mitigate | Prevent auto-replay/auto-resume from reconnect side effects |
</threat_model>

<verification>
Run the restore suite and confirm reconnect recovers state and control only, without automatic narration replay or resume.
</verification>

<success_criteria>
Reconnect and restore are safe:
- state and control restore correctly
- no auto-replay occurs
- no auto-resume occurs
</success_criteria>

<output>
After completion, create `.planning/phases/02-playback-state-reliability/02-playback-state-reliability-04-SUMMARY.md`
</output>
