---
phase: 02-playback-state-reliability
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - public/app.js
  - public/services/audio.js
  - server/routes/autoplex.js
  - tests/helpers/createAutoplexControlHarness.js
  - tests/runtime/playback-pause-resume.test.js
autonomous: true
requirements:
  - PLAY-01
  - PLAY-02
must_haves:
  truths:
    - "Pause is a hard freeze that stops new server progression and current browser audio."
    - "Resume continues the same slide from the paused point if technically possible."
    - "Late or stale audio chunks do not restart playback after pause or slide change."
  artifacts:
    - path: "public/services/audio.js"
      provides: "Deterministic hard-freeze pause/resume behavior and stale-playback cleanup."
    - path: "public/app.js"
      provides: "Browser orchestration aligned to hard-freeze semantics."
    - path: "tests/runtime/playback-pause-resume.test.js"
      provides: "Automated regression coverage for pause/resume behavior."
  key_links:
    - from: "public/app.js"
      to: "public/services/audio.js"
      via: "UI pause/resume actions control actual audio lifecycle"
      pattern: "toggleAudioPause|pause|resume"
    - from: "public/app.js"
      to: "server/routes/autoplex.js"
      via: "pause/resume server progression matches local audio freeze"
      pattern: "pauseAutoplex|/pause|/resume"
---

<objective>
Repair pause/resume and audio lifecycle behavior now that playback ownership is explicit.

Purpose: Implement D-05 through D-07 so pause behaves like a real freeze and no longer leaks scheduled or stale audio across state changes.
Output: Hardened client audio lifecycle plus pause/resume integration tests.
</objective>

<execution_context>
@/home/harsha-mudumba/.codex/get-shit-done/workflows/execute-plan.md
@/home/harsha-mudumba/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-playback-state-reliability/02-CONTEXT.md
@.planning/phases/02-playback-state-reliability/02-RESEARCH.md
@.planning/phases/02-playback-state-reliability/02-PATTERNS.md
@public/app.js
@public/services/audio.js
@server/routes/autoplex.js
@tests/runtime/playback-contract.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add hard-freeze pause/resume regression coverage</name>
  <read_first>
    - public/app.js
    - public/services/audio.js
    - server/routes/autoplex.js
    - .planning/phases/02-playback-state-reliability/02-PATTERNS.md
  </read_first>
  <files>tests/helpers/createAutoplexControlHarness.js, tests/runtime/playback-pause-resume.test.js</files>
  <behavior>
    - Test 1: pause stops current playback and prevents new queued chunks from rendering.
    - Test 2: resume continues the same slide contract rather than restarting a different one.
    - Test 3: slide changes or replay transitions clear stale paused audio state.
  </behavior>
  <action>Create a focused harness for `toggleAudioPause()`, `StreamAudioPlayer`, and the matching `/api/autoplex/pause` and `/api/autoplex/resume` semantics. Follow the patterns mapped in `02-PATTERNS.md`: keep `public/services/audio.js` as the actual playback authority, `public/app.js` as the control orchestrator, and use minimal server control stubs only where the pause contract crosses into `autoplex`.</action>
  <acceptance_criteria>
    - `test -f tests/helpers/createAutoplexControlHarness.js` succeeds.
    - `test -f tests/runtime/playback-pause-resume.test.js` succeeds.
    - `rg -n "hard freeze|stale|pause|resume" tests/runtime/playback-pause-resume.test.js` returns meaningful assertions.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand tests/runtime/playback-pause-resume.test.js</automated>
  </verify>
  <done>The repo has focused pause/resume regression coverage before behavior is changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce hard-freeze pause/resume and stale-audio cleanup</name>
  <read_first>
    - public/app.js
    - public/services/audio.js
    - server/routes/autoplex.js
    - tests/runtime/playback-pause-resume.test.js
  </read_first>
  <files>public/app.js, public/services/audio.js, server/routes/autoplex.js</files>
  <action>Refactor pause/resume so a pause freezes server progression and current audio consistently, while resume continues the same slide from the paused point when possible. Remove any remaining implicit fall-through where stale chunks or scheduled sources can continue after pause, replay, or slide change. Preserve the existing route and service boundaries; do not fold browser audio logic into the server.</action>
  <acceptance_criteria>
    - `rg -n "toggleAudioPause|pauseAutoplex|reset|hasPendingPlayback" public/app.js public/services/audio.js server/routes/autoplex.js` reflects explicit hard-freeze behavior.
    - `npm test -- --runInBand tests/runtime/playback-pause-resume.test.js` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand tests/runtime/playback-pause-resume.test.js</automated>
  </verify>
  <done>Pause/resume behaves like a true freeze and does not leak audio or progression across transitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI pause action -> browser audio engine | Pause must freeze rendered playback immediately |
| UI pause action -> server presentation loop | Pause must freeze progression, not only local sound output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | T | pause/resume lifecycle | mitigate | Make pause a hard freeze and test stale-chunk cleanup |
| T-02-04 | D | browser playback queue | mitigate | Prevent queued or late chunks from resuming after slide/state changes |
</threat_model>

<verification>
Run the pause/resume suite and confirm pause freezes both playback and progression, while resume continues the same slide contract without stale audio bleed.
</verification>

<success_criteria>
Hard-freeze playback semantics are in place:
- pause stops audio and progression
- resume continues the same slide where possible
- stale audio no longer leaks into new states
</success_criteria>

<output>
After completion, create `.planning/phases/02-playback-state-reliability/02-playback-state-reliability-02-SUMMARY.md`
</output>
