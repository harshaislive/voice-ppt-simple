---
phase: 02-playback-state-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/app.js
  - public/services/socket.js
  - server/routes/autoplex.js
  - server.js
  - tests/helpers/createPlaybackContractHarness.js
  - tests/runtime/playback-contract.test.js
autonomous: true
requirements:
  - PLAY-01
must_haves:
  truths:
    - "The runtime has one explicit playback contract instead of multiple inferred ownership paths."
    - "The server owns session and slide phase, while the controller client owns rendered playback acknowledgment."
    - "Playback completion acknowledgments are explicit, consistent, and tied to the active slide/controller session."
  artifacts:
    - path: "public/services/socket.js"
      provides: "Narrow explicit playback contract between client and server."
    - path: "public/app.js"
      provides: "Client-side playback orchestration aligned to the explicit contract."
    - path: "server/routes/autoplex.js"
      provides: "Server-side session/slide phase ownership and acknowledgment handling."
    - path: "tests/runtime/playback-contract.test.js"
      provides: "Automated verification for the contract and acknowledgment rules."
  key_links:
    - from: "public/services/socket.js"
      to: "server.js"
      via: "Socket.IO playback acknowledgment and join contract"
      pattern: "presentation-audio-complete|join-session"
    - from: "public/app.js"
      to: "server/routes/autoplex.js"
      via: "client completion flows match server wait/phase expectations"
      pattern: "notifyPlaybackComplete|waitForPlaybackCompletion"
---

<objective>
Make playback ownership explicit before changing pause/resume or slide sequencing.

Purpose: Implement D-01 through D-04 first so later plans can fix behavior against a stable contract rather than guessing who owns each state transition.
Output: A narrow playback event contract, aligned browser/server ownership rules, and focused tests that fail when the contract regresses.
</objective>

<execution_context>
@/home/harsha-mudumba/.codex/get-shit-done/workflows/execute-plan.md
@/home/harsha-mudumba/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-playback-state-reliability/02-CONTEXT.md
@.planning/phases/02-playback-state-reliability/02-RESEARCH.md
@.planning/phases/02-playback-state-reliability/02-PATTERNS.md
@public/app.js
@public/services/socket.js
@server/routes/autoplex.js
@server.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add playback contract harness and regression coverage</name>
  <read_first>
    - public/app.js
    - public/services/socket.js
    - server/routes/autoplex.js
    - server.js
    - .planning/phases/02-playback-state-reliability/02-CONTEXT.md
    - .planning/phases/02-playback-state-reliability/02-PATTERNS.md
  </read_first>
  <files>tests/helpers/createPlaybackContractHarness.js, tests/runtime/playback-contract.test.js</files>
  <behavior>
    - Test 1: the controller client acknowledges playback completion explicitly for the active slide only.
    - Test 2: stale or duplicate completion acknowledgments do not advance the wrong slide.
    - Test 3: the server wait path distinguishes between "audio emission finished" and "client playback finished".
  </behavior>
  <action>Create a minimal playback contract harness that exercises the existing Socket.IO join and completion flow without booting the full browser runtime. Follow the exact event-driven patterns mapped in `02-PATTERNS.md`: reuse the current `presentation-audio-complete` and `join-session` contract surface from `server.js`, model active-slide ownership the way `public/app.js` currently tracks it, and keep the test harness narrow to playback state transitions rather than full narration generation.</action>
  <acceptance_criteria>
    - `test -f tests/helpers/createPlaybackContractHarness.js` succeeds.
    - `test -f tests/runtime/playback-contract.test.js` succeeds.
    - `rg -n "presentation-audio-complete|stale|duplicate|active slide" tests/runtime/playback-contract.test.js` returns contract assertions.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand tests/runtime/playback-contract.test.js</automated>
  </verify>
  <done>The repo has focused playback contract coverage that fails when ownership or acknowledgment semantics drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Formalize browser/server playback ownership and contract events</name>
  <read_first>
    - public/app.js
    - public/services/socket.js
    - server/routes/autoplex.js
    - server.js
    - tests/runtime/playback-contract.test.js
    - .planning/phases/02-playback-state-reliability/02-RESEARCH.md
  </read_first>
  <files>public/app.js, public/services/socket.js, server/routes/autoplex.js, server.js</files>
  <action>Refactor the current playback flow so ownership is explicit: server owns session/slide phase, client owns rendered audio completion, and both coordinate through one narrow event contract. Keep the current Socket.IO transport and existing route/module boundaries; do not invent a second control path. Follow the analogs in `02-PATTERNS.md`: keep `public/services/socket.js` as the contract edge, keep `public/app.js` as the browser orchestrator, and keep `server/routes/autoplex.js` as the server phase owner and waiter coordinator.</action>
  <acceptance_criteria>
    - `rg -n "presentation-audio-complete|playback|ack" public/app.js public/services/socket.js server/routes/autoplex.js server.js` shows explicit contract handling.
    - `npm test -- --runInBand tests/runtime/playback-contract.test.js` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand tests/runtime/playback-contract.test.js</automated>
  </verify>
  <done>Playback ownership is explicit and stable enough for pause/resume and completion fixes to build on top of it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller browser -> server playback loop | Browser playback acknowledgments must not drift from server phase ownership |
| server audio emission -> rendered browser playback | Server completion must not be confused with actual client playback completion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | client/server playback contract | mitigate | Make playback ownership explicit and regression-test stale/duplicate acknowledgments |
| T-02-02 | D | completion wait path | mitigate | Distinguish emitted audio completion from rendered playback completion |
</threat_model>

<verification>
Run the playback contract suite and confirm server/client ownership is explicit and stale acknowledgments cannot advance the wrong slide.
</verification>

<success_criteria>
Phase 2 playback ownership foundation is in place:
- one explicit contract exists
- server slide/session phase ownership is clear
- client playback acknowledgment is explicit and test-protected
</success_criteria>

<output>
After completion, create `.planning/phases/02-playback-state-reliability/02-playback-state-reliability-01-SUMMARY.md`
</output>
