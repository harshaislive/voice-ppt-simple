---
phase: 02-playback-state-reliability
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - public/app.js
  - public/services/socket.js
  - server/routes/autoplex.js
  - server/routes/slides.js
  - tests/runtime/playback-advance.test.js
autonomous: true
requirements:
  - PLAY-02
  - PLAY-03
must_haves:
  truths:
    - "Slide completion prefers valid client playback acknowledgment."
    - "A guarded server fallback exists so sessions cannot hang forever on lost acknowledgments."
    - "Automatic advance, replay, and explicit jump/override paths are separated cleanly."
  artifacts:
    - path: "server/routes/autoplex.js"
      provides: "Guarded completion and advance sequencing."
    - path: "server/routes/slides.js"
      provides: "Explicit override and jump semantics separated from passive completion."
    - path: "tests/runtime/playback-advance.test.js"
      provides: "Regression coverage for completion, fallback, and advance sequencing."
  key_links:
    - from: "public/app.js"
      to: "server/routes/autoplex.js"
      via: "client completion acknowledgment drives normal advance"
      pattern: "notifyPlaybackComplete|waitForPlaybackFinish"
    - from: "server/routes/slides.js"
      to: "server/routes/autoplex.js"
      via: "manual jump/advance remains an explicit override path"
      pattern: "advance|jump|reason"
---

<objective>
Fix slide completion and advance rules after playback ownership and pause/resume semantics are stable.

Purpose: Implement D-08 through D-10 so slide advancement happens for the right reason at the right time, with a guarded fallback instead of hanging forever.
Output: Explicit completion/advance sequencing and regression coverage for normal, fallback, and override paths.
</objective>

<execution_context>
@/home/harsha-mudumba/.codex/get-shit-done/workflows/execute-plan.md
@/home/harsha-mudumba/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-playback-state-reliability/02-CONTEXT.md
@.planning/phases/02-playback-state-reliability/02-RESEARCH.md
@.planning/phases/02-playback-state-reliability/02-PATTERNS.md
@public/app.js
@public/services/socket.js
@server/routes/autoplex.js
@server/routes/slides.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add completion and advance regression coverage</name>
  <read_first>
    - public/app.js
    - public/services/socket.js
    - server/routes/autoplex.js
    - server/routes/slides.js
    - .planning/phases/02-playback-state-reliability/02-PATTERNS.md
  </read_first>
  <files>tests/runtime/playback-advance.test.js</files>
  <behavior>
    - Test 1: normal slide advance happens only after active client completion acknowledgment.
    - Test 2: missing acknowledgment uses the guarded server fallback instead of hanging forever.
    - Test 3: manual jump/advance remains an explicit override and does not masquerade as natural playback completion.
  </behavior>
  <action>Add a focused runtime suite around the completion handshake, server fallback, and explicit slide override behavior. Follow the exact analogs in `02-PATTERNS.md`: treat `public/app.js` and `public/services/socket.js` as the client completion edge, `server/routes/autoplex.js` as the normal progression owner, and `server/routes/slides.js` as the explicit override surface.</action>
  <acceptance_criteria>
    - `test -f tests/runtime/playback-advance.test.js` succeeds.
    - `rg -n "fallback|ack|advance|jump" tests/runtime/playback-advance.test.js` returns sequencing assertions.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand tests/runtime/playback-advance.test.js</automated>
  </verify>
  <done>The completion and advance rules are encoded in a focused regression suite before behavior changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Separate client completion, fallback completion, and explicit override paths</name>
  <read_first>
    - public/app.js
    - public/services/socket.js
    - server/routes/autoplex.js
    - server/routes/slides.js
    - tests/runtime/playback-advance.test.js
  </read_first>
  <files>public/app.js, public/services/socket.js, server/routes/autoplex.js, server/routes/slides.js</files>
  <action>Refactor slide progression so normal advancement uses valid client completion acknowledgment, stalled sessions use a guarded fallback path, and manual jump/advance remains an explicit override path with explicit reasons/state. Preserve the current route split between `autoplex` and `slides`; do not collapse all control into one giant handler.</action>
  <acceptance_criteria>
    - `rg -n "fallback|ack|override|reason" public/app.js public/services/socket.js server/routes/autoplex.js server/routes/slides.js` returns explicit sequencing hooks.
    - `npm test -- --runInBand tests/runtime/playback-advance.test.js` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand tests/runtime/playback-advance.test.js</automated>
  </verify>
  <done>Slide completion and progression no longer depend on hidden timing races and cannot deadlock on lost acknowledgments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client completion ack -> server slide progression | A lost or stale acknowledgment must not cause early advance or deadlock |
| explicit controller override -> automatic progression | Manual overrides must remain separate from passive completion logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | T | slide completion sequencing | mitigate | Use explicit ack + guarded fallback rather than timing inference |
| T-02-06 | D | progression loop | mitigate | Prevent deadlocks on lost acknowledgments while keeping overrides explicit |
</threat_model>

<verification>
Run the advance suite and confirm normal playback, fallback completion, and explicit jump/advance overrides stay distinct and deterministic.
</verification>

<success_criteria>
Slide completion and advance behavior is deterministic:
- normal completion uses client ack
- fallback prevents hangs
- explicit overrides remain clearly separate
</success_criteria>

<output>
After completion, create `.planning/phases/02-playback-state-reliability/02-playback-state-reliability-03-SUMMARY.md`
</output>
