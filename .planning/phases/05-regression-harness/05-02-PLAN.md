---
phase: 05-regression-harness
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - tests/helpers
  - tests/runtime
  - package.json
autonomous: true
requirements:
  - TEST-01
must_haves:
  truths:
    - "The main presentation journey is protected as a first-class regression flow."
    - "Reconnect/restore is protected as a second first-class regression flow."
    - "Integrated flow tests compose session, playback, Q&A, and completion behavior rather than duplicating slice tests."
  artifacts:
    - path: "tests/runtime"
      provides: "Integrated regression suites for the two high-value journeys."
    - path: "tests/helpers"
      provides: "Flow-level harness support for the integrated journeys."
  key_links:
    - from: "server/routes/session.js"
      to: "server/routes/autoplex.js"
      via: "session startup and presentation progression compose the main journey"
      pattern: "session/start|presentation_start|complete"
    - from: "server/routes/questions.js"
      to: "server/routes/autoplex.js"
      via: "Q&A interruption and resume behavior must remain stable inside the journey"
      pattern: "qa|interrupt|resume"
---

<objective>
Protect the two highest-value runtime journeys as integrated regressions.

Purpose: Implement D-01 through D-03 by proving the main presentation flow and reconnect/restore flow still hold together across the runtime seams.
Output: Two integrated regression suites backed by the shared harness foundation.
</objective>

<execution_context>
@/home/harsha-mudumba/.codex/get-shit-done/workflows/execute-plan.md
@/home/harsha-mudumba/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-regression-harness/05-CONTEXT.md
@.planning/phases/05-regression-harness/05-RESEARCH.md
@.planning/phases/05-regression-harness/05-PATTERNS.md
@.planning/phases/02-playback-state-reliability/02-CONTEXT.md
@.planning/phases/03-grounded-q&a-experience/03-CONTEXT.md
@.planning/phases/04-content-loading-and-scaling-foundations/04-CONTEXT.md
@server/routes/session.js
@server/routes/autoplex.js
@server/routes/questions.js
@tests/helpers
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add integrated regression coverage for the main presentation journey</name>
  <read_first>
    - server/routes/session.js
    - server/routes/autoplex.js
    - server/routes/questions.js
    - tests/runtime/playback-*.test.js
    - tests/runtime/qa-*.test.js
  </read_first>
  <files>tests/runtime</files>
  <action>Create an integration-level regression suite that exercises start session, presentation progression, Q&A interruption, resume/advance, and completion inside one coherent runtime flow. Use real route wiring and in-memory runtime state, but keep external providers stubbed.</action>
  <acceptance_criteria>
    - A regression suite exists for the main presentation journey.
    - The suite proves session start, Q&A interruption, progression, and completion compose correctly together.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:regression</automated>
  </verify>
  <done>The most important sales presentation flow is protected against cross-slice regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add integrated regression coverage for reconnect and restore</name>
  <read_first>
    - server/routes/session.js
    - public/app.js
    - tests/runtime/playback-restore.test.js
    - .planning/phases/02-playback-state-reliability/02-CONTEXT.md
  </read_first>
  <files>tests/runtime, tests/helpers</files>
  <action>Create an integration-level regression suite for reconnect/restore that proves restored sessions keep the correct presentation identity, preserve control semantics, avoid stale replay, and resume continuity cleanly.</action>
  <acceptance_criteria>
    - A regression suite exists for reconnect/restore behavior.
    - The suite covers restore without stale replay drift or wrong controller/runtime state.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:regression</automated>
  </verify>
  <done>The second critical runtime journey is now protected by integrated regression coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session bootstrap -> playback/Q&A/runtime orchestration | The main journey must stay coherent across multiple route and runtime slices |
| persisted session state -> restored runtime ownership | Restore must not reinterpret identity, playback status, or controller state incorrectly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | T | main runtime journey | mitigate | Add an integrated regression flow spanning start, interruption, progression, and completion |
| T-05-04 | T | reconnect/restore flow | mitigate | Add a second integrated regression flow proving continuity after restore |
</threat_model>

<verification>
Run the standard regression tier and confirm both high-value journeys remain stable across the real runtime seams.
</verification>

<success_criteria>
The critical runtime story is protected:
- the main journey is regression-tested end to end
- reconnect/restore is regression-tested end to end
- slice-level tests are supplemented by true cross-slice coverage
</success_criteria>

<output>
After completion, create `.planning/phases/05-regression-harness/05-regression-harness-02-SUMMARY.md`
</output>
