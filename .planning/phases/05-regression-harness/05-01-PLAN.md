---
phase: 05-regression-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/helpers
  - package.json
  - jest.config.js
  - .planning/phases/05-regression-harness/05-VALIDATION.md
autonomous: true
requirements:
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "The repo exposes explicit smoke, standard, and extended regression commands."
    - "Integrated regression tests reuse shared runtime harnesses instead of inventing a second testing architecture."
    - "Phase 5 creates a clear foundation the later regression files can build on."
  artifacts:
    - path: "tests/helpers"
      provides: "Shared regression harnesses for backend/session orchestration."
    - path: "package.json"
      provides: "Smoke, standard, and extended test commands."
    - path: "jest.config.js"
      provides: "Test grouping support aligned to the tiered suite contract."
  key_links:
    - from: "tests/helpers"
      to: "tests/runtime/regression-*.test.js"
      via: "shared harnesses power the integrated regression flows"
      pattern: "create*Harness|createTestApp"
    - from: "package.json"
      to: "jest.config.js"
      via: "command surface maps to tiered suite selection"
      pattern: "test:smoke|test:regression|test:extended"
---

<objective>
Lay down the regression harness foundation and the explicit suite contract before adding the larger flow tests.

Purpose: Implement D-04 through D-09 first so Phase 5 has one coherent harness layer and one clear confidence model.
Output: Shared helpers plus smoke/standard/extended commands that later plans can target directly.
</objective>

<execution_context>
@/home/harsha-mudumba/.codex/get-shit-done/workflows/execute-plan.md
@/home/harsha-mudumba/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-regression-harness/05-CONTEXT.md
@.planning/phases/05-regression-harness/05-RESEARCH.md
@.planning/phases/05-regression-harness/05-PATTERNS.md
@tests/helpers/createTestApp.js
@tests/helpers/createPlaybackContractHarness.js
@tests/helpers/createContentLoadingHarness.js
@package.json
@jest.config.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared regression harness foundation</name>
  <read_first>
    - tests/helpers/createTestApp.js
    - tests/helpers/createPlaybackContractHarness.js
    - tests/helpers/createContentLoadingHarness.js
    - .planning/phases/05-regression-harness/05-CONTEXT.md
  </read_first>
  <files>tests/helpers</files>
  <action>Create or extend shared helpers so integrated regression flows can mount real session, playback, Q&A, and content-loading seams with in-memory state and stubbed external providers. Reuse the current helper style instead of building a new framework.</action>
  <acceptance_criteria>
    - `rg -n "create.*Harness" tests/helpers` shows a coherent integrated harness layer for session/runtime flows.
    - New helpers are obviously reusable by later regression suites.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:smoke</automated>
  </verify>
  <done>The repo has a reusable regression harness foundation that later plans can build on without copy-pasting setup logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define smoke, standard, and extended regression commands</name>
  <read_first>
    - package.json
    - jest.config.js
    - .planning/phases/05-regression-harness/05-VALIDATION.md
  </read_first>
  <files>package.json, jest.config.js, .planning/phases/05-regression-harness/05-VALIDATION.md</files>
  <action>Introduce the three-level suite contract through explicit commands or Jest grouping so developers know what to run for fast feedback, normal regression, and broader pre-ship coverage. Keep the contract simple and aligned to the existing Jest setup.</action>
  <acceptance_criteria>
    - `cat package.json` shows explicit smoke, standard regression, and extended commands.
    - `npm run test:smoke` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:smoke</automated>
  </verify>
  <done>The repo has a concrete confidence model instead of one ambiguous `npm test` command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helper layer -> regression suites | Shared harnesses must be realistic enough to catch real regressions without pulling in live providers |
| command surface -> developer workflow | Suite tiers must be obvious enough that developers actually run the right tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | I | regression harness foundation | mitigate | Reuse and extend current helper patterns rather than inventing a disconnected test architecture |
| T-05-02 | D | test command contract | mitigate | Expose explicit smoke, standard, and extended commands aligned to the actual suite tiers |
</threat_model>

<verification>
Run the smoke tier and confirm the harness foundation and command contract are in place and usable.
</verification>

<success_criteria>
Phase 5 starts from a solid foundation:
- shared helpers support integrated regression work
- smoke/standard/extended tiers are explicit
- later regression suites can target a known command surface
</success_criteria>

<output>
After completion, create `.planning/phases/05-regression-harness/05-regression-harness-01-SUMMARY.md`
</output>
