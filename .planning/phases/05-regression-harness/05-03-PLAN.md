---
phase: 05-regression-harness
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - tests/runtime
  - package.json
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - TEST-02
must_haves:
  truths:
    - "Security/startup failures are protected in the regression contract."
    - "Runtime continuity failures are protected in the regression contract."
    - "Extended regression confidence does not depend on live providers."
  artifacts:
    - path: "tests/runtime"
      provides: "Negative-path suites for startup/security and runtime continuity failures."
    - path: "package.json"
      provides: "Extended regression command aligned to the negative-path gate."
  key_links:
    - from: "tests/runtime/*auth*.test.js"
      to: "tests/runtime/content-*.test.js"
      via: "security and startup denial paths form one negative-path gate"
      pattern: "403|fail closed|invalid startup"
    - from: "tests/runtime/playback-*.test.js"
      to: "tests/runtime/qa-*.test.js"
      via: "continuity failures span restore, stale playback, interruption, and completion boundaries"
      pattern: "restore|pause|qa|complete"
---

<objective>
Finish Phase 5 by formalizing the negative-path regression gate.

Purpose: Implement D-10 through D-13 so startup/security failures and runtime continuity failures are both protected under the explicit regression contract.
Output: Extended negative-path coverage and a completed Phase 5 regression story without live-provider dependency.
</objective>

<execution_context>
@/home/harsha-mudumba/.codex/get-shit-done/workflows/execute-plan.md
@/home/harsha-mudumba/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-regression-harness/05-CONTEXT.md
@.planning/phases/05-regression-harness/05-RESEARCH.md
@.planning/phases/05-regression-harness/05-PATTERNS.md
@tests/runtime/*auth*.test.js
@tests/runtime/content-*.test.js
@tests/runtime/playback-*.test.js
@tests/runtime/qa-*.test.js
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend negative-path regression coverage across startup/security and continuity failures</name>
  <read_first>
    - tests/runtime/cms-auth.test.js
    - tests/runtime/analytics-auth.test.js
    - tests/runtime/session-control-auth.test.js
    - tests/runtime/content-fail-closed.test.js
    - tests/runtime/playback-restore.test.js
    - tests/runtime/qa-isolation.test.js
  </read_first>
  <files>tests/runtime</files>
  <action>Expand the negative-path regression layer so it covers both startup/security denials and runtime continuity failures. This should include failure cases like invalid startup, wrong-source loading, reconnect drift, stale playback after interruption, and broken completion or cleanup boundaries.</action>
  <acceptance_criteria>
    - Negative-path suites exist for both startup/security and runtime continuity failures.
    - The extended regression tier clearly includes those failure classes.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:extended</automated>
  </verify>
  <done>The regression contract protects both denial paths and runtime continuity failures, not just happy paths.</done>
</task>

<task type="auto">
  <name>Task 2: Sync regression contract artifacts and phase completion state</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/05-regression-harness/05-VALIDATION.md
  </read_first>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <action>After the regression tiers and negative-path coverage are in place, sync the planning artifacts so the repo reflects the new test contract and Phase 5 readiness cleanly.</action>
  <acceptance_criteria>
    - Planning artifacts reflect the completed regression harness shape.
    - Extended regression command and coverage intent are discoverable from the repo state.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:extended</automated>
  </verify>
  <done>The planning layer and regression contract stay aligned instead of drifting after implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| startup/auth/content denial paths -> runtime entry | Invalid requests must fail explicitly and stay protected by regression coverage |
| interruption/restore/completion boundaries -> session continuity | Runtime continuity failures must not slip through because only happy paths were tested |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | I | startup/security failures | mitigate | Group and extend denial-path tests into the explicit regression contract |
| T-05-06 | T | runtime continuity failures | mitigate | Add extended regression coverage for reconnect drift, stale playback, and broken cleanup/completion behavior |
</threat_model>

<verification>
Run the extended tier and confirm Phase 5 protects both startup/security denials and runtime continuity failures without relying on live providers.
</verification>

<success_criteria>
Phase 5 is complete when:
- negative-path regression coverage is explicit and broad enough
- security/startup and continuity failures are both protected
- the repo exposes a maintainable three-tier regression contract
</success_criteria>

<output>
After completion, create `.planning/phases/05-regression-harness/05-regression-harness-03-SUMMARY.md`
</output>
