---
phase: 01-runtime-guardrails
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - package.json
  - server.js
  - server/config/runtime.js
  - server/middleware/security.js
  - tests/helpers/createRuntimeConfigTestEnv.js
  - tests/runtime/runtime-config.test.js
autonomous: true
requirements:
  - OPS-02
must_haves:
  truths:
    - "Production boot stops immediately when auth, CORS, AI, or required persistence config is unsafe."
    - "Staging behaves like production unless an explicit, named, non-production override is configured."
    - "`DISABLE_SESSION_CONTROL` cannot silently bypass controller protection outside local development."
  artifacts:
    - path: "server/config/runtime.js"
      provides: "Environment classification and fail-closed runtime validation."
    - path: "server/middleware/security.js"
      provides: "Restricted session-control bypass rules aligned with runtime environment classification."
    - path: "tests/runtime/runtime-config.test.js"
      provides: "Automated coverage for invalid production/staging config and local-only bypass behavior."
  key_links:
    - from: "server.js"
      to: "server/config/runtime.js"
      via: "startup validateRuntimeConfig() and createCorsOptions() flow"
      pattern: "validateRuntimeConfig\\(\\)"
    - from: "server/middleware/security.js"
      to: "process.env.DISABLE_SESSION_CONTROL"
      via: "local-only bypass gating"
      pattern: "DISABLE_SESSION_CONTROL"
---

<objective>
Make unsafe runtime configuration fail closed before the server starts, with staging behavior close to production and no unrestricted session-control bypass outside local development.

Purpose: Implement D-04, D-05, and D-06 at the existing startup and middleware chokepoints instead of relying on convention or environment discipline.
Output: Hardened runtime config validation, stricter CORS behavior, restricted session-control bypass rules, and tests for invalid boot states.
</objective>

<execution_context>
@/home/harsha-mudumba/.codex/get-shit-done/workflows/execute-plan.md
@/home/harsha-mudumba/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-runtime-guardrails/01-CONTEXT.md
@.planning/phases/01-runtime-guardrails/01-RESEARCH.md
@.planning/phases/01-runtime-guardrails/01-PATTERNS.md
@server/config/runtime.js
@server/middleware/security.js
@server.js
@package.json

<interfaces>
From server/config/runtime.js:
```javascript
function parseAllowedOrigins() { ... }
function validateRuntimeConfig() { ... }
function createCorsOptions(allowedOrigins = []) { ... }
```

From server/middleware/security.js:
```javascript
async function hasValidSessionControlAsync(db, sessionId, providedToken) { ... }
```

From server.js:
```javascript
const runtimeConfig = validateRuntimeConfig();
const corsOptions = createCorsOptions(runtimeConfig.allowedOrigins);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add automated runtime-config regression coverage</name>
  <read_first>
    - package.json
    - server/config/runtime.js
    - server/middleware/security.js
    - server.js
    - .planning/phases/01-runtime-guardrails/01-CONTEXT.md
    - .planning/phases/01-runtime-guardrails/01-RESEARCH.md
    - .planning/phases/01-runtime-guardrails/01-PATTERNS.md
  </read_first>
  <files>tests/helpers/createRuntimeConfigTestEnv.js, tests/runtime/runtime-config.test.js</files>
  <behavior>
    - Test 1: production config without `ALLOWED_ORIGINS` throws `INVALID_RUNTIME_CONFIG` per D-04.
    - Test 2: staging config follows the same required-provider/auth checks unless an explicit named test override is enabled per D-05.
    - Test 3: `DISABLE_SESSION_CONTROL=true` is rejected or ignored outside local development per D-06.
    - Test 4: `createCorsOptions()` denies unknown origins for production-class environments.
  </behavior>
  <action>Create a runtime-config test helper that can safely set and restore process env for CommonJS module reloads, then add Jest coverage around `server/config/runtime.js` and `server/middleware/security.js` behavior. Follow the exact-module analogs mapped in `01-PATTERNS.md`: keep the tests focused on the existing startup-validation and CORS policy shape in `server/config/runtime.js`, and exercise the session-control bypass logic through the existing helper layout in `server/middleware/security.js`. Keep this suite pure-module level; do not boot the full server process in tests.</action>
  <acceptance_criteria>
    - `test -f tests/runtime/runtime-config.test.js` succeeds.
    - `rg -n "INVALID_RUNTIME_CONFIG|DISABLE_SESSION_CONTROL|createCorsOptions" tests/runtime/runtime-config.test.js` returns matches.
    - The test helper resets env/module state between cases so cases are isolated.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand tests/runtime/runtime-config.test.js</automated>
  </verify>
  <done>The repo has repeatable automated coverage for the specific unsafe runtime states Phase 1 must block.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden production and staging runtime validation plus local-only bypass rules</name>
  <read_first>
    - server/config/runtime.js
    - server/middleware/security.js
    - server.js
    - tests/runtime/runtime-config.test.js
    - .planning/phases/01-runtime-guardrails/01-CONTEXT.md
    - .planning/phases/01-runtime-guardrails/01-PATTERNS.md
  </read_first>
  <files>package.json, server.js, server/config/runtime.js, server/middleware/security.js</files>
  <action>Refactor `server/config/runtime.js` to expose one explicit environment classifier that reads `APP_ENV` first and falls back to `NODE_ENV`, with `local` and `development` treated as local development, `staging`, `preview`, and `preprod` treated as production-class staging, and `production` treated as production. Enforce D-04 and D-05 by requiring safe auth, CORS, provider, and remote-persistence settings for both staging and production through that classifier, and change `createCorsOptions()` so production-class environments fail closed on unknown origins instead of returning `origin: true`. Update `server/middleware/security.js` so `hasValidSessionControlAsync()` only honors `DISABLE_SESSION_CONTROL` in clearly local development and either ignores or throws on that flag elsewhere per D-06. Follow the exact analogs mapped in `01-PATTERNS.md`: extend the existing environment parsing and startup validation pattern already present in `server/config/runtime.js`, and keep `server.js` limited to consuming the new runtime-config shape without duplicating validation logic.</action>
  <acceptance_criteria>
    - `rg -n "function (classify|resolve).*Environment|environment" server/config/runtime.js` returns an explicit environment-classification helper.
    - `rg -n "Origin not allowed by CORS" server/config/runtime.js` still exists in the deny path.
    - `rg -n "DISABLE_SESSION_CONTROL" server/middleware/security.js` shows a local-only guard rather than an unconditional bypass.
    - `npm test -- --runInBand tests/runtime/runtime-config.test.js` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand tests/runtime/runtime-config.test.js</automated>
  </verify>
  <done>Unsafe production/staging configs fail before boot, CORS is fail-closed for production-class environments, and session-control bypass is limited to explicit local development only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process env -> server boot | Untrusted or incomplete deployment config must not produce a partially secure runtime |
| environment flags -> session-control middleware | Ops toggles must not disable controller authorization in staging or production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | T | `server/config/runtime.js` | mitigate | Throw `INVALID_RUNTIME_CONFIG` when production-class auth, CORS, provider, or required persistence settings are unsafe per D-04/D-05 |
| T-01-06 | E | `server/middleware/security.js` | mitigate | Restrict `DISABLE_SESSION_CONTROL` to local development only per D-06 |
| T-01-07 | I | `server/config/runtime.js` CORS policy | mitigate | Deny unknown origins in production-class environments instead of permissive fallback |
| T-01-08 | R | deployment safety | mitigate | Add module-level Jest coverage for invalid boot and bypass scenarios so unsafe env drift is regression-tested |
</threat_model>

<verification>
Run the runtime-config suite and confirm invalid production/staging env combinations fail, unknown production origins are denied, and `DISABLE_SESSION_CONTROL` no longer acts as a broad escape hatch.
</verification>

<success_criteria>
Phase 1 fail-closed boot posture is in place:
- startup rejects unsafe configuration
- staging mirrors production restrictions unless an explicit test override is named
- controller bypass cannot be enabled outside local development
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-guardrails/01-runtime-guardrails-02-SUMMARY.md`
</output>
