---
phase: 01-runtime-guardrails
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - server/routes/session.js
  - server/routes/autoplex.js
  - server/routes/questions.js
  - server/routes/analytics.js
  - server/services/analytics.js
  - tests/runtime/session-diagnostics.test.js
autonomous: true
requirements:
  - OPS-03
must_haves:
  truths:
    - "Operators can correlate session start, source selection, playback/Q&A lifecycle, and session end with consistent structured fields."
    - "Presentation and Q&A failures emit machine-readable diagnostics tied to the session timeline."
    - "Lifecycle logging uses the shared logger contract rather than introducing per-route schemas."
  artifacts:
    - path: "server/routes/session.js"
      provides: "Session-start, session-load, source-selection, and session-end diagnostics."
    - path: "server/routes/autoplex.js"
      provides: "Presentation lifecycle and failure diagnostics tied to session timelines."
    - path: "server/routes/questions.js"
      provides: "Q&A lifecycle and failure diagnostics tied to session timelines."
    - path: "server/services/analytics.js"
      provides: "Timeline/event persistence coordination using the structured logger contract."
    - path: "tests/runtime/session-diagnostics.test.js"
      provides: "Automated verification that lifecycle event names and bindings stay consistent."
  key_links:
    - from: "server/routes/session.js"
      to: "server/routes/autoplex.js"
      via: "shared sessionId event naming across lifecycle logs"
      pattern: "sessionId"
    - from: "server/routes/questions.js"
      to: "server/services/analytics.js"
      via: "structured Q&A diagnostics and event persistence coordination"
      pattern: "(qa|question)"
    - from: "server/routes/analytics.js"
      to: "server/middleware/logger.js"
      via: "reuse shared logger bindings and event schema rather than route-local console output"
      pattern: "logger"
---

<objective>
Instrument the major session lifecycle routes and analytics service with the shared logger so operators can reconstruct what happened during a presentation.

Purpose: Finish D-08 on top of the logger foundation from Plan 01-03 without mixing base logger setup and route/service instrumentation into one oversized execution slice.
Output: Route/service lifecycle instrumentation plus expanded diagnostics tests covering session start, source selection, presentation, Q&A, and session end events.
</objective>

<execution_context>
@/home/harsha-mudumba/.codex/get-shit-done/workflows/execute-plan.md
@/home/harsha-mudumba/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-runtime-guardrails/01-CONTEXT.md
@.planning/phases/01-runtime-guardrails/01-RESEARCH.md
@.planning/phases/01-runtime-guardrails/01-PATTERNS.md
@server/routes/session.js
@server/routes/autoplex.js
@server/routes/questions.js
@server/routes/analytics.js
@server/services/analytics.js
@tests/runtime/session-diagnostics.test.js

<interfaces>
From server/routes/session.js:
```javascript
router.post('/start', async (req, res) => { ... });
router.get('/:id', requireSessionControl({ keys: ['id'] }), async (req, res) => { ... });
```

From server/routes/autoplex.js:
```javascript
const activePresentationRuns = new Map();
function markInterrupted(sessionId) { ... }
function setPaused(sessionId, paused) { ... }
```

From server/routes/questions.js:
```javascript
async function loadQuestionAnswerContext(db, sessionId) { ... }
async function persistQuestionAnswer({ db, io, question, answerText, audioResult, sessionId }) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend diagnostics tests for session lifecycle events</name>
  <read_first>
    - tests/runtime/session-diagnostics.test.js
    - server/routes/session.js
    - server/routes/autoplex.js
    - server/routes/questions.js
    - server/routes/analytics.js
    - server/services/analytics.js
    - .planning/phases/01-runtime-guardrails/01-CONTEXT.md
    - .planning/phases/01-runtime-guardrails/01-PATTERNS.md
  </read_first>
  <files>tests/runtime/session-diagnostics.test.js</files>
  <behavior>
    - Test 1: session start emits a structured lifecycle log with a stable event name and `sessionId`.
    - Test 2: source selection or session load emits a structured event tied to the same session timeline.
    - Test 3: Q&A or presentation failure logging emits an error event with subsystem and session identifiers.
    - Test 4: session completion or teardown emits a final timeline event using the shared logger schema.
  </behavior>
  <action>Expand the existing diagnostics suite so it asserts the lifecycle events required by D-08 on top of the logger/redaction guarantees from Plan 01-03. Follow the route and service analogs mapped in `01-PATTERNS.md`: preserve the current handler shapes in `server/routes/session.js`, `server/routes/autoplex.js`, and `server/routes/analytics.js`, and use `server/routes/slides.js` as the partial analog for `server/routes/questions.js` request-response flow. Keep assertions on stable event names, `sessionId`, subsystem, and high-signal metadata only.</action>
  <acceptance_criteria>
    - `rg -n "session_start|session_end|source_|presentation_|qa_" tests/runtime/session-diagnostics.test.js` returns lifecycle assertions.
    - `npm test -- --runInBand tests/runtime/session-diagnostics.test.js` currently fails before instrumentation is added.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand tests/runtime/session-diagnostics.test.js</automated>
  </verify>
  <done>The diagnostics suite explicitly encodes the session lifecycle events operators must be able to reconstruct.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instrument session, playback, Q&A, and analytics flows with shared structured logging</name>
  <read_first>
    - server/routes/session.js
    - server/routes/autoplex.js
    - server/routes/questions.js
    - server/routes/analytics.js
    - server/services/analytics.js
    - tests/runtime/session-diagnostics.test.js
    - .planning/phases/01-runtime-guardrails/01-CONTEXT.md
    - .planning/phases/01-runtime-guardrails/01-PATTERNS.md
  </read_first>
  <files>server/routes/session.js, server/routes/autoplex.js, server/routes/questions.js, server/routes/analytics.js, server/services/analytics.js, tests/runtime/session-diagnostics.test.js</files>
  <action>Replace the highest-value `console.*` diagnostics in `server/routes/session.js`, `server/routes/autoplex.js`, `server/routes/questions.js`, `server/routes/analytics.js`, and `server/services/analytics.js` with stable event names that cover session start, auth-adjacent denials already emitted by middleware, source selection, presentation lifecycle, Q&A lifecycle, analytics persistence coordination, and session end per D-08. Reuse the shared logger contract from Plan 01-03 rather than creating route-local schemas. Follow the analogs in `01-PATTERNS.md`: preserve the existing session bootstrap and protected read shapes in `server/routes/session.js`, keep `server/routes/autoplex.js` event-driven state helpers intact, preserve the request-response flow in `server/routes/questions.js`, and keep `server/services/analytics.js` as the event sink instead of moving persistence into routes.</action>
  <acceptance_criteria>
    - `rg -n "(session_start|session_end|source_|presentation_|qa_)" server/routes/session.js server/routes/autoplex.js server/routes/questions.js server/routes/analytics.js server/services/analytics.js` returns timeline event names.
    - `rg -n "logger|child\\(" server/routes/session.js server/routes/autoplex.js server/routes/questions.js server/routes/analytics.js server/services/analytics.js` returns shared-logger usage.
    - `npm test -- --runInBand tests/runtime/session-diagnostics.test.js` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand tests/runtime/session-diagnostics.test.js</automated>
  </verify>
  <done>Operators can reconstruct core runtime behavior from consistent structured lifecycle logs without route-specific schema drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route/service failures -> diagnostics stream | Failure events must be machine-readable and correlated by session rather than scattered console text |
| lifecycle events -> operator interpretation | Event names and bindings must stay consistent enough for humans and tooling to reconstruct the session timeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | R | session lifecycle diagnostics | mitigate | Emit stable structured event names with `sessionId` and subsystem bindings across session, playback, and Q&A flows per D-08 |
| T-01-14 | D | incident debugging path | mitigate | Replace critical route/service `console.*` traces with machine-readable structured logs tied to the shared logger contract |
| T-01-15 | T | analytics/log schema drift | accept | This phase keeps diagnostics in-process and route-scoped; schema evolution can continue later as long as shared event names and bindings stay stable |
</threat_model>

<verification>
Run the diagnostics suite and confirm structured logs exist for session start, source selection, presentation, Q&A, analytics coordination, and session end using the shared logger schema.
</verification>

<success_criteria>
Phase 1 lifecycle instrumentation is in place:
- session timelines can be reconstructed across major lifecycle events
- presentation and Q&A failures are logged with session-aware structured fields
- route/service diagnostics reuse the shared logger contract
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-guardrails/01-runtime-guardrails-04-SUMMARY.md`
</output>
