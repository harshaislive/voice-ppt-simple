---
phase: 01-runtime-guardrails
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - package.json
  - package-lock.json
  - server.js
  - server/middleware/logger.js
  - server/middleware/security.js
  - tests/helpers/createLoggerTestApp.js
  - tests/runtime/session-diagnostics.test.js
autonomous: true
requirements:
  - OPS-03
must_haves:
  truths:
    - "Core app and security diagnostics use the shared machine-readable logger foundation instead of scattered `console.*` output."
    - "Auth denial and other security-adjacent log events carry stable fields without exposing raw admin/session token values."
    - "Routes and services have a shared logger entry point that later lifecycle instrumentation can bind to with consistent keys."
  artifacts:
    - path: "server/middleware/logger.js"
      provides: "Shared structured logger with child bindings and redaction."
    - path: "server.js"
      provides: "Application-level logger wiring that makes the structured logger available to routes and middleware."
    - path: "tests/runtime/session-diagnostics.test.js"
      provides: "Automated verification for structured output, child bindings, and redaction rules."
  key_links:
    - from: "server.js"
      to: "server/middleware/logger.js"
      via: "app-level logger initialization and request/app access"
      pattern: "logger"
    - from: "server/middleware/security.js"
      to: "server/middleware/logger.js"
      via: "structured auth-denial logging with redacted sensitive fields"
      pattern: "(auth_|logger)"
---

<objective>
Establish the shared structured logger foundation and wire it into the runtime before instrumenting the broader session lifecycle.

Purpose: Implement the D-07 and D-09 foundations first so the later lifecycle instrumentation can reuse one logger contract instead of scattering logging behavior across routes.
Output: Shared logger middleware/utilities, app/security wiring for structured events, and automated verification for schema/redaction guarantees.
</objective>

<execution_context>
@/home/harsha-mudumba/.codex/get-shit-done/workflows/execute-plan.md
@/home/harsha-mudumba/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-runtime-guardrails/01-CONTEXT.md
@.planning/phases/01-runtime-guardrails/01-RESEARCH.md
@.planning/phases/01-runtime-guardrails/01-PATTERNS.md
@server.js
@server/middleware/security.js
@package.json

<interfaces>
From server/middleware/security.js:
```javascript
function requireSessionControl(options = {}) { ... }
function requireAdminApiKey(req, res, next) { ... }
```

From server.js:
```javascript
app.set('db', db);
app.use('/api/session', sessionRoutes);
app.use('/api/questions', questionRoutes);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add structured-logger verification harness and redaction tests</name>
  <read_first>
    - package.json
    - package-lock.json
    - server.js
    - server/middleware/security.js
    - .planning/phases/01-runtime-guardrails/01-CONTEXT.md
    - .planning/phases/01-runtime-guardrails/01-RESEARCH.md
    - .planning/phases/01-runtime-guardrails/01-PATTERNS.md
  </read_first>
  <files>package.json, package-lock.json, tests/helpers/createLoggerTestApp.js, tests/runtime/session-diagnostics.test.js</files>
  <behavior>
    - Test 1: the base logger emits structured JSON with a stable `event` field and child bindings for `sessionId` and `subsystem`.
    - Test 2: an auth failure path emits a structured denial log without raw admin keys or session-control tokens.
    - Test 3: redaction removes participant names, prompt-heavy content, and auth headers from captured log payloads per D-09.
  </behavior>
  <action>Make dependency setup explicit before logger verification: add the exact logger dependencies needed for this slice to `package.json` and `package-lock.json` by installing `pino` as a runtime dependency and `pino-pretty` as a dev-only dependency, then create a logger-focused test harness that captures structured log output from a minimal app/middleware composition and asserts schema plus redaction rules. Follow the patterns mapped in `01-PATTERNS.md`: reuse `server.js` as the wiring analog for app-level middleware composition, and exercise `server/middleware/security.js` through its existing middleware-first contract instead of inventing a parallel auth path. Keep the harness limited to logger behavior; it does not need full playback, provider, or Socket.IO flows.</action>
  <acceptance_criteria>
    - `rg -n "\"pino\"" package.json package-lock.json` returns runtime dependency matches.
    - `rg -n "\"pino-pretty\"" package.json package-lock.json` returns dev dependency matches.
    - `test -f tests/runtime/session-diagnostics.test.js` succeeds.
    - `rg -n "event|sessionId|subsystem|redact|authorization|participantName|prompt|token" tests/runtime/session-diagnostics.test.js` returns schema and no-leak assertions.
  </acceptance_criteria>
  <verify>
    <automated>npm ls pino && npm ls --include=dev pino-pretty && npm test -- --runInBand tests/runtime/session-diagnostics.test.js</automated>
  </verify>
  <done>There is automated verification for the structured log schema and redaction guarantees that Phase 1 requires before broader instrumentation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the shared logger module and wire security/app logging</name>
  <read_first>
    - server.js
    - server/middleware/security.js
    - tests/runtime/session-diagnostics.test.js
    - .planning/phases/01-runtime-guardrails/01-CONTEXT.md
    - .planning/phases/01-runtime-guardrails/01-PATTERNS.md
  </read_first>
  <files>package.json, server.js, server/middleware/logger.js, server/middleware/security.js</files>
  <action>Add a shared `pino` logger module with child logger helpers and redaction for authorization headers, admin keys, session control tokens, participant names, and prompt-heavy fields per D-07 and D-09. Wire the logger through `server.js` so routes and middleware can access one shared logger instance, and add structured auth-denial logging in `server/middleware/security.js` without changing the existing allow/deny semantics. Follow the analogs in `01-PATTERNS.md`: mirror the current startup-wiring shape in `server.js`, preserve the middleware-first helper layout in `server/middleware/security.js`, and keep event keys stable so later route/service instrumentation can layer on top instead of redefining the schema.</action>
  <acceptance_criteria>
    - `test -f server/middleware/logger.js` succeeds.
    - `rg -n "pino|child\\(|redact" server/middleware/logger.js server.js server/middleware/security.js` returns structured-logger usage.
    - `rg -n "auth_" server/middleware/security.js` returns structured auth event names.
    - `npm test -- --runInBand tests/runtime/session-diagnostics.test.js` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand tests/runtime/session-diagnostics.test.js</automated>
  </verify>
  <done>The runtime has one structured logger entry point with redaction and auth-denial logging, ready for lifecycle instrumentation in the next plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal runtime state -> operator logs | Logs must be useful for diagnosis without leaking secrets or sensitive freeform content |
| auth middleware decisions -> diagnostics stream | Denied requests must be traceable without revealing raw credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | I | `server/middleware/logger.js` | mitigate | Redact admin/session tokens, participant names, and prompt-heavy fields in the shared logger per D-09 |
| T-01-10 | R | `server/middleware/security.js` | mitigate | Emit stable structured auth event names from security middleware so denials are diagnosable without raw credential leakage |
| T-01-11 | D | app logging foundation | mitigate | Replace ad hoc `console.*` on core security/app boundaries with machine-readable structured logs per D-07 |
| T-01-12 | T | logger schema | accept | This plan fixes the base schema and redaction contract; later lifecycle instrumentation may add fields but should not redefine core bindings |
</threat_model>

<verification>
Run the diagnostics suite and confirm the logger emits structured JSON, child bindings are available, and sensitive fields are redacted on auth/security events.
</verification>

<success_criteria>
Phase 1 structured logging foundation is in place:
- one shared logger module exists
- app and security middleware emit structured events instead of raw console output
- sensitive auth/participant/prompt fields are redacted
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-guardrails/01-runtime-guardrails-03-SUMMARY.md`
</output>
